name: Matrix demonstration
on: workflow_dispatch
jobs:
    DemoMatrix:
        runs-on: ubuntu-latest
        strategy:
            matrix:
                color: ["green", "pink"]
                animal: ["owl", "magpie"]
                include:
                    - color: blue
                      animal: owl
                    - color: pink
                      animal: magpie
                exclude:
                    - color: green

        steps:
            - run: echo ${{matrix.color}}
